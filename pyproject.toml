[build-system]
requires = ["setuptools"]

[project]
name = "FinanceGPT"
readme = "README.md"
requires-python = ">=3.11"

dynamic = ["version"]

dependencies = [
]

test = [
    "black==23.7.0",
    "pytest==7.4.0",
    "flake8==6.1.0",
    "mypy==1.4.1"
]


[tool.setuptools.dynamic]
version = {attr = "financegpt.__version__"}